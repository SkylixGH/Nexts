import {Settings} from '../window/Window'

/**
 * Create a new window in the app.
 * @param settings The window settings.
 * @returns The new window.
 */
export function create(settings: Settings) {

}
